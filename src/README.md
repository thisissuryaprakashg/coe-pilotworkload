# Source Code

This folder contains the executable Python code for the CogPilot workload project.

- `data_pipeline/` cleans signals, applies quality rules, and assembles datasets.
- `modeling/` screens features, trains models, and evaluates LOSO benchmarks.

Run scripts from the repository root with `python src/...`. The scripts resolve project files relative to their own location.
