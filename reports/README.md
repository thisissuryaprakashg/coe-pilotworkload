# Reports

This folder contains generated analysis outputs rather than source code.

It includes feature-screening tables, selected feature definitions, mathematical-model coefficients and summaries, LOSO benchmark results, and enhanced-model comparisons.

Most files can be regenerated by scripts in `src/modeling/`.
