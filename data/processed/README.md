# Processed Data

Generated modeling inputs are stored here:

- `cleaned_multimodal_dataset.csv`: cleaned ILS runs after quality exclusions.
- `master_feature_matrix.csv`: cleaned runs merged with rest-baseline deltas, eye features, labels, and performance metrics.

These files can be regenerated by the scripts in `src/data_pipeline/`.
