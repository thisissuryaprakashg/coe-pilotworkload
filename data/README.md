# Data

Data files used or generated by the project belong here.

- `raw/` is reserved for the downloaded CogPilot dataset and is not included in this repository.
- `processed/` contains cleaned and assembled CSV tables generated by the pipeline.

Keep source recordings in `raw/` and generated tables in `processed/` so inputs and outputs remain easy to distinguish.
